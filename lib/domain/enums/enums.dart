enum DiscountType {
  percentage,
  value,
}

enum AccessLevel {
  staff,
  admin,
}

enum DataStatus {
  initial,
  loading,
  success,
  error,
}

enum FormStatus {
  initial,
  loading,
  loaded,
  validating,
  invalid,
  valid,
  submitting,
  submitted,
  error,
}

enum ProductFormStatus {
  initial,
  validating,
  invalid,
  valid,
  submittingCategories,
  submittingProduct,
  submittingUnits,
  submitted,
  error,
}
